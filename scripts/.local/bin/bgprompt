#!/bin/sh

walldir="$HOME/wallpapers"

choice="$(ls "$walldir" | rofi -dmenu -i -p "Choose a background")"
[ -z "$choice" ] && exit

if [ "$choice" = "Random" ]; then
	choice="$walldir"
else
	choice=$(printf "$walldir/%s" "$choice")
fi

setbg "$choice"
